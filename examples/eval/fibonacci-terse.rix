## A compact, stack-safe Fibonacci implementation.

Fibonacci(n, a ?= 0, b ?= 1) ->
    n <= 0 ?: a ?_ $(n - 1, b, a + b);

{:
    Fibonacci(0),
    Fibonacci(1),
    Fibonacci(10),
    Fibonacci(100)
}

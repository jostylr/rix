## Collatz using a case container and a tail-self call.

NextCollatz(n) -> {?
    n % 2 == 0 ? n / 2;
    3 * n + 1
};

CollatzLength(n, count) -> {?
    n == 1 ? count;
    $(NextCollatz(n), count + 1)
};

StepsToOne(n) -> CollatzLength(n, 0);

{= start=27, steps=StepsToOne(27) }

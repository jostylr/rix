## Prime discovery with collection pipes.

HasDivisor(n) ->
    .IRANGE(2, .Sqrt(n) // 1) |>|| ((divisor) -> n % divisor == 0);

IsPrime(n) -> n < 2 ?: _ ?_ n == 2 ?: 1 ?_ !HasDivisor(n);

primes := .IRANGE(1, 50) |>? IsPrime;

{= primes=primes, count=.LEN(primes) }
